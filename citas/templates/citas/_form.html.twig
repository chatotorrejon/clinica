<div style="margin-left: auto; margin-right: auto; text-align: center; padding-top: 100px; border: 2px solid blue; background-color: black; color: white; width: 500px; height: 300px;">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        
        <button class="btn">{{ button_label|default('Save') }}</button>

    {{ form_end(form) }}
</div>